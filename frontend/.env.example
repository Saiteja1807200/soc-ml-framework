# Frontend environment variables (used by Vite at build time)
# Copy to .env and set your production API URL

# API Base URL — set to your deployed backend URL in production
VITE_API_URL=http://localhost:8000/api
